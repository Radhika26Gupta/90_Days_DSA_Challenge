#include<iostream>
#include<vector>
using namespace std;
int main()
{
    vector<int> arr;//need not to mention the size 
    arr.push_back(8);
    arr.push_back(9);
    arr.push_back(6);
    arr.push_back(3);
    cout<<arr[0];
    cout<<arr[1];
    cout<<arr[2];
    cout<<arr[3];
}
